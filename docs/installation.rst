============
Installation
============

At the command line::

    $ easy_install django-layer-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-layer-auth
    $ pip install django-layer-auth